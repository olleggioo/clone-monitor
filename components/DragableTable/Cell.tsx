import { FC } from "react"
import { TableCellI } from "../Table/Table"

const Cell: FC<TableCellI> = ({
    onClick,
    onLink,
    url,
    title,
    accessor,
    icon,
    description,
    additionalDescription,
    status,
    bold, 
    align,
    width,
    isLoading,
    state,
  }) => {
    return <td>
        
    </td>
}

export default Cell